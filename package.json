{
  "name": "acai",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/worker",
    "packages/shared"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @acai/web",
    "dev:worker": "npm run dev --workspace @acai/worker",
    "build:web": "npm run build --workspace @acai/web",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}

